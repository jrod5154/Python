# In this script I find the outliers In a Data Set, and display them.

upper_limit = 91
lower_limit = 24
outlier = False
number = 8

if number > upper_limit:
  outlier = True
if number < lower_limit:
  outlier = True
if outlier == True:
  print(f"{number} is an outlier")

Answer:
8 is an outlier
