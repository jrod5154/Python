# Here I create a script from a Data Base to show if there are enough data points, or if the program has to keep collecting.

minimum_points = 100
data_points = 150

if data_points >= minimum_points:
  print("There are enough data points!")
if data_points < minimum_points:
  print("Keep collecting data.")
